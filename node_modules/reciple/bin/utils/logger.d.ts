import { Logger, LoggerLevel, PartialDeep } from 'fallout-utility';
import { IConfig } from '../classes/Config';
import { RecipleClient } from '../';
export declare function formatLogMessage(message: string, logger: Logger, config: PartialDeep<IConfig['logger']>, level: LoggerLevel): string;
export declare function createLogger(config: PartialDeep<IConfig['logger']>): Promise<Logger>;
export declare function quoteString(string: string, quote?: string): string;
export declare function eventLogger(client: RecipleClient): void;
