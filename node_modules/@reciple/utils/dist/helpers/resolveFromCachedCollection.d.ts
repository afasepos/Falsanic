import { Collection } from '@discordjs/collection';
/**
 * Resolve item from cache or fetch if not cached
 * @param id Item id
 * @param manager Item manager
 */
export declare function resolveFromCachedCollection<V>(id: string, manager: {
    cache: Collection<string, V>;
    fetch(key: string): Promise<V | null>;
}): Promise<V>;
