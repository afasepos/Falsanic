import { MessageCommandOptionValue } from '../builders/MessageCommandOptionBuilder';
import { Collection } from 'discord.js';
export declare class MessageCommandOptionManager extends Collection<string, MessageCommandOptionValue> {
    /**
     * Get an option data
     * @param option Option name
     * @param full Returns the full option data if true
     */
    getOption(option: string, full?: false): string | undefined;
    getOption(option: string, full?: true): MessageCommandOptionValue;
    /**
     * Get option value
     * @param optionName Option name
     * @param required This option is required
     */
    getValue(optionName: string, required?: false): string | null;
    getValue(optionName: string, required?: true): string;
}
