import { AnyCommandBuilder, AnyCommandData, AnyCommandExecuteFunction, CommandType } from '../../types/commands';
import { PermissionResolvable } from 'discord.js';
export declare class CommandAssertions {
    static validateCommandType(commandType: unknown): asserts commandType is CommandType;
    static validateCommandCooldown(cooldown: unknown): asserts cooldown is number;
    static validateCommandRequiredBotPermissions(permissions: unknown): asserts permissions is PermissionResolvable;
    static validateCommandRequiredMemberPermissions(permissions: unknown): asserts permissions is PermissionResolvable;
    static validateCommandExecute(execute: unknown): asserts execute is AnyCommandExecuteFunction;
    static validateCommandHalt(halt: unknown): asserts halt is AnyCommandExecuteFunction;
    static validateCommand(command: unknown): asserts command is (AnyCommandData | AnyCommandBuilder);
}
